{{!-- <section>
    <div class="container">
        <div class="row mt-4">
            <a href="/user/cart" class="btn btn-success ml-auto">Shopping Cart</a>
        </div>
        <table class="table mt-5">
            <thead>
                <tr>
                    <th scope="col:">Sl No</th>
                    <th scope="col:">Item</th>
                    <th scope="col:">Title</th>
                    <th scope="col:">Quantity</th>
                    <th scope="col:">Price</th>
                </tr>
            </thead>
            <tbody>
                {{#each products}}
                <tr>
                    <td scope="row">{{@index}}</td>
                     <td><img style="width: 50px; height:50px" src="/product-images/{{this._id}}.jpg" alt="Image"></td>
                    <td>{{this.Title}}</td>
                    <td>
                        <button class="cart-item-count mr-3">-</button>1<button class="cart-item-count ml-3">+</button>
                    </td>
                    <td>{{this.Price}}</td>
                   
                    <td><a href="/admin/delete-product/{{this._id}}" class="btn btn-danger" onclick="return confirm('Do you want to delete {{this.Name}} ?')">Delete</a>
                        </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</section> --}}

<section>
    <div class="container">
        <table class="table mt-5">
            <thead>
                <tr>
                    <th scope="col">Item</th>
                    <th scope="col">Title</th>
                    <th scope="col">Quantity</th>
                </tr>
            </thead>
            <tbody>
                {{#each products}}
                <tr>
                    <td><img style="width: 50px; height:50px" src="/product-images/{{this._id}}.jpg" alt="Image"></td>
                    <td>{{this.Name}}</td>
                    <td><button class="cart-item-count mr-3">-</button>1<button class="cart-item-count ml-3">+</button></td>
                    <td><a href="" class="btn btn-danger">Delete</a></td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</section>